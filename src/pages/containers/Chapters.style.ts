import { ChapterWidth } from "../../utils/Layout";

const ChaptersStyle = {
  root: {
    flexGrow: 1,
    display: "flex",
    maxWidth: ChapterWidth,
    alignItems: "center",
    flexDirection: "column"
  }
};

export default ChaptersStyle